# Whisper 語音轉文字（使用 GitHub 安裝，不要寫在這裡）
# ffmpeg 與相依套件
ffmpeg-python==0.2.0
numpy==1.26.4
scipy==1.11.4
tqdm==4.66.2

# PyTorch for CUDA 12.1 (你已成功安裝)
# torch==2.5.1+cu121
# torchaudio==2.5.1+cu121
# torchvision==0.20.1+cu121
# 使用 pip install --index-url https://download.pytorch.org/whl/cu121 安裝

# Streamlit Web UI
streamlit==1.31.0
requests==2.31.0

# LangChain 套件（版本需相容）
langchain==0.1.17
langchain-openai==0.1.6
langchain-community==0.0.36

# Word 摘要處理
docxtpl==0.16.7
python-docx==1.1.0
